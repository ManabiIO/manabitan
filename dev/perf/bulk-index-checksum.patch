diff --git a/ext/js/dictionary/term-key-hash.js b/ext/js/dictionary/term-key-hash.js
index b2a514d..7fdf457 100644
--- a/ext/js/dictionary/term-key-hash.js
+++ b/ext/js/dictionary/term-key-hash.js
@@ -107,3 +107,51 @@ function readUint32(bytes, offset) {
         (bytes[offset + 3] << 24)
     ) >>> 0;
 }
+
+/**
+ * The same seeded XXH32 as hashTermKeyBytes, with bulk little-endian word reads
+ * for large persisted index sections. This is not a new hash or storage format.
+ * @param {Uint8Array} bytes
+ * @returns {number}
+ */
+export function hashTermKeyBytesBulk(bytes) {
+    // Lookup-index sections are large, unlike the keys on the query hot path.
+    // Keep that path allocation-free; this one amortizes a DataView over a section.
+    const start = 0;
+    const end = bytes.byteLength;
+    const view = new DataView(bytes.buffer, bytes.byteOffset, bytes.byteLength);
+    let offset = start;
+    const length = end - start;
+    let hash;
+    if (length >= 16) {
+        const limit = end - 16;
+        let v1 = (HASH_SEED + PRIME1 + PRIME2) >>> 0;
+        let v2 = (HASH_SEED + PRIME2) >>> 0;
+        let v3 = HASH_SEED;
+        let v4 = (HASH_SEED - PRIME1) >>> 0;
+        do {
+            v1 = round(v1, view.getUint32(offset, true)); offset += 4;
+            v2 = round(v2, view.getUint32(offset, true)); offset += 4;
+            v3 = round(v3, view.getUint32(offset, true)); offset += 4;
+            v4 = round(v4, view.getUint32(offset, true)); offset += 4;
+        } while (offset <= limit);
+        hash = (rotateLeft(v1, 1) + rotateLeft(v2, 7) + rotateLeft(v3, 12) + rotateLeft(v4, 18)) >>> 0;
+    } else {
+        hash = (HASH_SEED + PRIME5) >>> 0;
+    }
+    hash = (hash + length) >>> 0;
+    while (offset + 4 <= end) {
+        hash = Math.imul(rotateLeft((hash + Math.imul(view.getUint32(offset, true), PRIME3)) >>> 0, 17), PRIME4) >>> 0;
+        offset += 4;
+    }
+    while (offset < end) {
+        hash = Math.imul(rotateLeft((hash + Math.imul(bytes[offset], PRIME5)) >>> 0, 11), PRIME1) >>> 0;
+        ++offset;
+    }
+    hash ^= hash >>> 15;
+    hash = Math.imul(hash, PRIME2) >>> 0;
+    hash ^= hash >>> 13;
+    hash = Math.imul(hash, PRIME3) >>> 0;
+    hash ^= hash >>> 16;
+    return hash >>> 0;
+}
diff --git a/ext/js/dictionary/term-record-opfs-store.js b/ext/js/dictionary/term-record-opfs-store.js
index d15bcc6..5c5d947 100644
--- a/ext/js/dictionary/term-record-opfs-store.js
+++ b/ext/js/dictionary/term-record-opfs-store.js
@@ -15,6 +15,7 @@
  * along with this program.  If not, see <https://www.gnu.org/licenses/>.
  */
 
+import {hashTermKeyBytesBulk} from './term-key-hash.js';
 import {reportDiagnostics} from '../core/diagnostics-reporter.js';
 import {safePerformance} from '../core/safe-performance.js';
 import {toError} from '../core/to-error.js';
@@ -405,7 +406,7 @@ function getContentOffsetDelta(offset, base) {
  * @returns {number}
  */
 function hashLookupIndexBytes(bytes) {
-    return hashTermLookupKeyBytes(bytes);
+    return hashTermKeyBytesBulk(bytes);
 }
 
 /**
diff --git a/test/term-key-hash-bulk.test.js b/test/term-key-hash-bulk.test.js
new file mode 100644
index 0000000..02c6dd3
--- /dev/null
+++ b/test/term-key-hash-bulk.test.js
@@ -0,0 +1,90 @@
+/*
+ * Copyright (C) 2026 Manabitan Authors
+ *
+ * This program is free software: you can redistribute it and/or modify
+ * it under the terms of the GNU General Public License as published by
+ * the Free Software Foundation, either version 3 of the License, or
+ * (at your option) any later version.
+ */
+
+import {describe, expect, test} from 'vitest';
+import {hashTermKeyByteRange, hashTermKeyBytes, hashTermKeyBytesBulk} from '../ext/js/dictionary/term-key-hash.js';
+
+/**
+ * @param {number} length
+ * @returns {Uint8Array}
+ */
+function createBytes(length) {
+    return Uint8Array.from({length}, (_, i) => (Math.imul(i, 131) + (i >>> 8)) & 255);
+}
+
+describe('bulk persisted-index XXH32', () => {
+    test.each([false, true])('matches scalar hashing for every short length and alignment (shared=%s)', (shared) => {
+        const storage = shared ? new SharedArrayBuffer(1032) : new ArrayBuffer(1032);
+        const whole = new Uint8Array(storage);
+        whole.set(createBytes(whole.length));
+        const before = Uint8Array.from(whole);
+        for (let offset = 0; offset < 8; ++offset) {
+            for (let length = 0; length <= 1024; ++length) {
+                const view = whole.subarray(offset, offset + length);
+                expect(hashTermKeyBytesBulk(view)).toBe(hashTermKeyBytes(view));
+                expect(hashTermKeyBytesBulk(view)).toBe(hashTermKeyByteRange(whole, offset, offset + length));
+            }
+        }
+        expect(whole).toStrictEqual(before);
+    });
+
+    test.each([4095, 4096, 4097, 65535, 65536, 65537, 1048573, 1048576, 1048579])('preserves index checksums for %i bytes and all word tails', (length) => {
+        const source = createBytes(length + 32);
+        for (let offset = 0; offset < 8; ++offset) {
+            for (let tail = 0; tail < 16; ++tail) {
+                const view = source.subarray(offset, offset + length + tail);
+                expect(hashTermKeyBytesBulk(view)).toBe(hashTermKeyBytes(view));
+            }
+        }
+    });
+
+    test('hashes shared WASM views before and after memory growth', () => {
+        const memory = new WebAssembly.Memory({initial: 1, maximum: 2, shared: true});
+        const source = new Uint8Array(memory.buffer, 3, 65530);
+        source.set(createBytes(source.length));
+        const expected = hashTermKeyBytes(source);
+        expect(hashTermKeyBytesBulk(source)).toBe(expected);
+        memory.grow(1);
+        expect(hashTermKeyBytesBulk(source)).toBe(expected);
+        const grown = new Uint8Array(memory.buffer, 3, 100003);
+        grown.set(createBytes(grown.length));
+        expect(hashTermKeyBytesBulk(grown)).toBe(hashTermKeyBytes(grown));
+    });
+
+    test('matches the native XXH32 oracle with the persisted seed', () => {
+        // Generated by libxxhash XXH32(bytes, length, 0x811c9dc5), not by either
+        // JavaScript implementation. The byte pattern is defined above.
+        const vectors = [
+            [0, 0x6a7ec0f8],
+            [1, 0x87862717],
+            [3, 0x68624aa0],
+            [4, 0x176a88a5],
+            [7, 0x98959cc3],
+            [8, 0x63605c04],
+            [15, 0x97a56b01],
+            [16, 0xcbba3b01],
+            [17, 0xe21930d5],
+            [31, 0xda50e52b],
+            [32, 0x18d89d90],
+            [33, 0xbfbd7d17],
+            [255, 0x0ee1e006],
+            [256, 0x6dbd9522],
+            [257, 0x4341e322],
+            [4095, 0xd42b482b],
+            [4096, 0x0b8ba369],
+            [4097, 0x1aaccac1],
+            [65535, 0x744ae86c],
+            [65536, 0x9d3204ed],
+            [1048579, 0x3092157d],
+        ];
+        for (const [length, expected] of vectors) {
+            expect(hashTermKeyBytesBulk(createBytes(length))).toBe(expected);
+        }
+    });
+});
