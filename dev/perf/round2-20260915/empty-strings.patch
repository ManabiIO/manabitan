diff --git a/ext/js/dictionary/wasm/term-bank-parser.c b/ext/js/dictionary/wasm/term-bank-parser.c
index 5b57eb7..d83a05d 100644
--- a/ext/js/dictionary/wasm/term-bank-parser.c
+++ b/ext/js/dictionary/wasm/term-bank-parser.c
@@ -393,6 +393,10 @@ static int is_hex_digit(uint8_t value) {
 static int parse_string_span(const uint8_t* src, uint32_t len, uint32_t start, uint32_t* out_end) {
     if (start >= len || src[start] != '"') { return 0; }
     uint32_t i = start + 1u;
+    if (i < len && src[i] == '"') {
+        *out_end = i + 1u;
+        return 1;
+    }
     while (i < len) {
         while (i + 8u <= len) {
             uint64_t word;
