diff --git a/ext/js/dictionary/term-content-block-store.js b/ext/js/dictionary/term-content-block-store.js
index 48cd7d7c..31577078 100644
--- a/ext/js/dictionary/term-content-block-store.js
+++ b/ext/js/dictionary/term-content-block-store.js
@@ -1411,14 +1411,31 @@ function packContentSpansIntoSlabs(sourceBytes, sourceOffsets, sourceLengths, ta
         const packedIndex = packedChunks.length;
         const output = new Uint8Array(totalBytes);
         let outputOffset = 0;
+        let runStart = 0;
+        let runEnd = 0;
+        let runOutputOffset = 0;
         for (let i = startIndex; i < endIndex; ++i) {
             const sourceOffset = sourceOffsets[i];
             const length = sourceLengths[i];
             sourceChunkIndices[i] = packedIndex;
             sourceChunkLocalOffsets[i] = outputOffset;
-            output.set(sourceBytes.subarray(sourceOffset, sourceOffset + length), outputOffset);
+            // Keep per-entry reference metadata, but copy adjacent source bytes
+            // together. Empty entries do not interrupt an otherwise adjacent run.
+            if (length > 0) {
+                if (sourceOffset !== runEnd) {
+                    if (runEnd > runStart) {
+                        output.set(sourceBytes.subarray(runStart, runEnd), runOutputOffset);
+                    }
+                    runStart = sourceOffset;
+                    runOutputOffset = outputOffset;
+                }
+                runEnd = sourceOffset + length;
+            }
             outputOffset += length;
         }
+        if (runEnd > runStart) {
+            output.set(sourceBytes.subarray(runStart, runEnd), runOutputOffset);
+        }
         packedChunks.push(output);
         startIndex = endIndex;
     }
