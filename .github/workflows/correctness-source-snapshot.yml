name: Correctness source snapshot

on:
  push:
    branches:
      - review/correctness-deep-20260921
  workflow_dispatch:

permissions:
  contents: read

jobs:
  snapshot:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Preserve exact source
        run: |
          mkdir -p builds/correctness-source
          git rev-parse HEAD > builds/correctness-source/commit.txt
          git archive --format=tar.gz --output=builds/correctness-source/source.tar.gz HEAD
      - uses: actions/upload-artifact@v4
        with:
          name: correctness-source
          path: builds/correctness-source/
          retention-days: 7
          if-no-files-found: error
