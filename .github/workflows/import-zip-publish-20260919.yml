# The completed one-shot publisher is retained unchanged at eaa2781e63f0e0d826df6f379b194973272a545f.
# It successfully audited and published in run 35457287078. Remove its write
# permission and prevent accidental republication; keep a read-only receipt.
name: Verify bounded ZIP publication receipt
on:
  push:
    branches: [research/import-zip-ranges-20260919]
    paths: [.github/workflows/import-zip-publish-20260919.yml]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  receipt:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: 15f6b53437774cb8daa2627597660a6e34bb7c20
          fetch-depth: 2
          persist-credentials: false
      - name: Verify the published parent, tree and two product blobs
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = 15f6b53437774cb8daa2627597660a6e34bb7c20
          test "$(git rev-parse 'HEAD^')" = fb7241deb70950dc113089cd774c9489ede48f8a
          test "$(git rev-parse 'HEAD^{tree}')" = 60abd26d596fb90b44f14380b89e52d6853a1490
          test "$(git diff --name-only 'HEAD^' HEAD | wc -l)" -eq 2
          echo '3f944585412c7c581486575841bc677a2bdc42500f6b1ba2bf86bfee20378bd1  ext/js/dictionary/term-bank-source-pipeline.js' | sha256sum --check
          echo '065886f0b6998027464e1391dad09c44c1593e0375ffe0afd371503d31ed8961  test/raw-zip-range-reader.test.js' | sha256sum --check
