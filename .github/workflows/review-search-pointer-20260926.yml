name: Archived search pointer experiment
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disposition:
    runs-on: ubuntu-24.04
    timeout-minutes: 1
    steps:
      - name: Retain the failed experiment without a publication path
        run: |
          echo 'Rejected as a focus fix. No product branch was published.'
          echo 'Original recipe: d4c155cc2c15a38d6c511c3248af601da26b4b93'
          echo 'Qualification run: 36228151631'
          echo 'Baseline passed; the cache composition still failed a focus assertion.'
          echo 'Historical source and complete results remain available at those immutable identities.'
