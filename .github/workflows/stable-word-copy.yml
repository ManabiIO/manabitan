name: Stable shared-byte copy verification
on:
  push:
    branches: [verify/stable-word-copy-20260910]
    paths:
      - .github/workflows/stable-word-copy.yml
      - dev/perf/stable-word-copy/**
permissions:
  contents: read
jobs:
  correctness:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true
      - uses: actions/setup-node@v4
        with:
          node-version: 24.20.0
          cache: npm
      - run: npm ci
      - name: Reconstruct exact source-only candidate
        shell: bash
        run: |
          set -euo pipefail
          evidence="$RUNNER_TEMP/evidence"
          mkdir -p "$evidence"
          echo '8bfd1bb8a85c0ae988cf828412564eb7ccdbe57e5a3d8971f4cdf7d7cd14a23b  dev/perf/stable-word-copy/source.patch' | sha256sum -c -
          cp dev/perf/stable-word-copy/source.patch "$evidence/source.patch"
          git worktree add --detach "$RUNNER_TEMP/candidate" d5dd9cb0072059b326f333ef425018ce1227a756
          cd "$RUNNER_TEMP/candidate"
          git apply --index "$evidence/source.patch"
          test "$(git write-tree)" = 8385544b3e978ea4c3140d78a27c1e31118392f4
          git -c core.hooksPath=/dev/null -c user.name='Manabitan verification' -c user.email='verification@users.noreply.github.com' commit -m 'perf(import): copy stable shared content with integer loads'
          cp -al "$GITHUB_WORKSPACE/node_modules" node_modules
          git rev-parse HEAD 'HEAD^{tree}' > "$evidence/source.txt"
          node dev/bin/build-libs.js > "$evidence/build-libs.log" 2>&1
      - name: Full tests, options, strict types, lint and builds
        shell: bash
        run: |
          set -euo pipefail
          cd "$RUNNER_TEMP/candidate"
          node node_modules/vitest/vitest.mjs run > "$RUNNER_TEMP/evidence/unit.log" 2>&1
          node node_modules/vitest/vitest.mjs run --config test/data/vitest.options.config.json > "$RUNNER_TEMP/evidence/options.log" 2>&1
          npm run test:ts > "$RUNNER_TEMP/evidence/types.log" 2>&1
          npm run test:js > "$RUNNER_TEMP/evidence/lint.log" 2>&1
          node dev/bin/build.js --dryRun --all > "$RUNNER_TEMP/evidence/dry-build.log" 2>&1
          test -z "$(git status --porcelain)"
      - name: Publish tested source to verification branch only
        shell: bash
        run: |
          set -euo pipefail
          cd "$RUNNER_TEMP/candidate"
          git push origin HEAD:refs/heads/verify/stable-word-copy-source-20260910
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: stable-word-copy-correctness
          path: ${{ runner.temp }}/evidence/
          retention-days: 14
          if-no-files-found: error
  comparison:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        dictionary: [jmdict, jmnedict, jitendex]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: 24.20.0
          cache: npm
      - run: npm ci
      - run: node node_modules/playwright/cli.js install --with-deps chromium
      - name: Build immutable source arms and record actual native memory policy
        shell: bash
        run: |
          set -euo pipefail
          evidence="$RUNNER_TEMP/evidence"
          arms="$RUNNER_TEMP/arms"
          mkdir -p "$evidence" "$arms"
          echo '8bfd1bb8a85c0ae988cf828412564eb7ccdbe57e5a3d8971f4cdf7d7cd14a23b  dev/perf/stable-word-copy/source.patch' | sha256sum -c -
          cp dev/perf/stable-word-copy/source.patch "$evidence/source.patch"
          cp dev/perf/stable-word-copy/paired.py "$evidence/paired.py"
          git worktree add --detach "$arms/A" d5dd9cb0072059b326f333ef425018ce1227a756
          git worktree add --detach "$arms/B" d5dd9cb0072059b326f333ef425018ce1227a756
          git -C "$arms/B" apply --index "$evidence/source.patch"
          test "$(git -C "$arms/B" write-tree)" = 8385544b3e978ea4c3140d78a27c1e31118392f4
          git -C "$arms/B" -c core.hooksPath=/dev/null -c user.name='Manabitan verification' -c user.email='verification@users.noreply.github.com' commit -m 'perf(import): copy stable shared content with integer loads'
          for arm in A B; do
            cp -al "$GITHUB_WORKSPACE/node_modules" "$arms/$arm/node_modules"
            (cd "$arms/$arm" && node dev/bin/build-libs.js && node dev/bin/build.js --target chrome-dev) > "$evidence/build-$arm.log" 2>&1
            test -z "$(git -C "$arms/$arm" status --porcelain)"
          done
          (cd "$arms/A" && node dev/perf/prepare-dictionaries.js) > "$evidence/fixtures.log" 2>&1
          ln -s "$arms/A/builds/e2e-dictionary-cache" "$arms/B/builds/e2e-dictionary-cache"
          node --input-type=module <<'NODE' > "$evidence/native-memory.json"
          import {createServer} from 'node:http';
          import {chromium} from 'playwright';
          const server = createServer((req, res) => res.end('<!doctype html><title>memory preflight</title>'));
          await new Promise(resolve => server.listen(0, '127.0.0.1', resolve));
          const browser = await chromium.launch({headless: true});
          try {
            const page = await browser.newPage();
            await page.goto(`http://127.0.0.1:${server.address().port}`);
            const policy = await page.evaluate(async () => {
              const url = URL.createObjectURL(new Blob(['postMessage({deviceMemory: navigator.deviceMemory, hardwareConcurrency: navigator.hardwareConcurrency})'], {type: 'text/javascript'}));
              const worker = new Worker(url);
              const value = await new Promise(resolve => worker.onmessage = event => resolve(event.data));
              worker.terminate();
              URL.revokeObjectURL(url);
              return {page: {deviceMemory: navigator.deviceMemory, hardwareConcurrency: navigator.hardwareConcurrency}, worker: value};
            });
            console.log(JSON.stringify({browserVersion: browser.version(), ...policy}, null, 2));
          } finally { await browser.close(); server.close(); }
          NODE
          clang --version > "$evidence/compiler.txt"
          node --version > "$evidence/node.txt"
          lscpu > "$evidence/cpu.txt"
          free -b > "$evidence/memory.txt"
      - name: Six fixed pairs with warmups and same-binary controls, no retries
        shell: bash
        env:
          DICTIONARY: ${{ matrix.dictionary }}
        run: |
          set -euo pipefail
          python3 dev/perf/stable-word-copy/paired.py --baseline "$RUNNER_TEMP/arms/A" --candidate "$RUNNER_TEMP/arms/B" --dictionary "$DICTIONARY" --pairs 6 --output "$RUNNER_TEMP/evidence/paired" 2>&1 | tee "$RUNNER_TEMP/evidence/driver.log"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: stable-word-copy-${{ matrix.dictionary }}
          path: ${{ runner.temp }}/evidence/
          retention-days: 14
          if-no-files-found: error
