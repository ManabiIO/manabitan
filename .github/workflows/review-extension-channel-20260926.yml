name: Archived Chromium channel focus experiment
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disposition:
    runs-on: ubuntu-24.04
    timeout-minutes: 1
    steps:
      - name: Retain the failed experiment without a publication path
        run: |
          echo 'The full Chromium channel did not resolve the search focus/input-loss failure.'
          echo 'No product branch or test assertion changes were published.'
          echo 'Original recipe: f1e24e60a96a240437207e9fbc9859c200aa0207'
          echo 'Qualification run: 36228805921'
          echo 'Baseline headed and both cache lanes still failed. Baseline headless passed.'
          echo 'The cache headless lane includes three focus failures and one empty-value failure.'
          echo 'Historical source and complete results remain available at those immutable identities.'
