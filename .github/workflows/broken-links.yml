name: Broken Link Checker

permissions:
  contents: read

# runs on prs containing markdown or html changes, as well as every monday at 9 am
on:
  pull_request:
    paths:
      - "**.md"
      - "**.html"
  schedule:
    - cron: "0 9 * * 1"

jobs:
  link-checker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Install dependencies
        run: npm ci
      - name: Build Legal
        run: npm run license-report:html
      - name: Determine link-check inputs
        id: inputs
        shell: bash
        run: |
          args="--exclude-path node_modules --verbose --no-progress './**/*.md' './**/*.html' './**/*.rst'"
          if [[ "${GITHUB_EVENT_NAME}" == "pull_request" ]]; then
            git diff --name-only "${{ github.event.pull_request.base.sha }}" "${{ github.event.pull_request.head.sha }}" -- '*.md' '*.html' '*.rst' > .lychee-files
            if [[ -s .lychee-files ]]; then
              args="--exclude-path node_modules --verbose --no-progress --files-from .lychee-files"
            fi
          fi
          printf 'args=%s\n' "${args}" >> "${GITHUB_OUTPUT}"
      - uses: lycheeverse/lychee-action@v2.6.1
        with:
          fail: true
          jobSummary: false
          args: ${{ steps.inputs.outputs.args }}
